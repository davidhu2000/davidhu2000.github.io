---
import { getPosts } from "../helpers/getPosts";

const posts = getPosts().slice(0, 4);
---

<section class="px-4 py-16 mx-auto max-w-6xl lg:py-20">
  <div class="flex flex-col mb-6 lg:justify-between lg:flex-row md:mb-8">
    <a
      href="/blog"
      class="link px-0 max-w-lg mb-2 font-sans text-3xl font-bold tracking-tight sm:text-4xl sm:leading-none lg:mb-5 group item-center flex-row"
    >
      <span class="inline-block mb-1">My latest blog posts</span>
    </a>
  </div>

  <div class="grid gap-6 row-gap-5 md:grid-cols-2 lg:grid-cols-4 -mb-6">
    {
      posts.map((post) => (
        <div class="mb-6 transition">
          <a
            href={post.url}
            class="link decoration-1 transition ease-in duration-200 p-0"
          >
            <img
              src={post.frontmatter.heroImage}
              class="object-cover w-full h-64 mb-6 rounded shadow-lg hover:ring-4 hover:ring-sky-400 dark:hover:ring-sky-500 transition duration-300"
              alt={post.frontmatter.title}
            />
          </a>
          <h3 class="mb-2 text-xl font-bold leading-snug sm:text-2xl">
            <a
              href={post.url}
              class="link decoration-1 transition ease-in duration-200 p-0"
            >
              {post.frontmatter.title}
            </a>
          </h3>
          <p class="text-gray-700 dark:text-gray-400">
            {post.frontmatter.description}
          </p>
        </div>
      ))
    }
  </div>
</section>
